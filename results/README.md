# Results

This folder contains saved outputs generated from the bundled example runs.

- `validation_output.txt`
  - output from `python examples/run_validation.py`
- `debug_trace_output.txt`
  - output from `python examples/run_debug_trace.py`

The idea is simple: a reviewer can inspect both the clean parity run and the shorter debug-oriented run without executing anything first.
